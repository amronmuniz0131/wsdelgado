export const metadata = {
    title: "Change Password | WSDelgado Builders",
    description: "Change Password WSDelgado Builders management system",
};

export default function ChangePasswordLayout({ children }) {
    return <>{children}</>;
}
