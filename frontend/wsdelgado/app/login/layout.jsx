export const metadata = {
  title: "Login | WSDelgado Builders",
  description: "Secure login for WSDelgado Builders management system. Access your dashboard, projects, and employee records.",
};

export default function LoginLayout({ children }) {
  return <>{children}</>;
}
