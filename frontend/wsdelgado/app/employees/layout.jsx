export const metadata = {
  title: "Employee Management | WSDelgado Builders",
  description: "Manage WSDelgado Builders workforce. Track employee positions, project assignments, and employment status.",
};

export default function EmployeesLayout({ children }) {
  return <>{children}</>;
}
