export const metadata = {
  title: "Dashboard | WSDelgado Builders",
  description: "WSDelgado Builders Project Management Dashboard. Overview of ongoing projects, materials, and equipment.",
};

export default function DashboardLayout({ children }) {
  return <>{children}</>;
}
